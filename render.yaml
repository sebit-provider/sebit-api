services:
  - type: web
    name: sebit-engine-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn models.app.main:app --host 0.0.0.0 --port $PORT
